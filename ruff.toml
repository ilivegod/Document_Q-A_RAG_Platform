[lint]
select = ["E", "F"]
ignore = [
    "E402",  # Module level import not at top — intentional for Sentry init
]

[lint.per-file-ignores]
"app/models/__init__.py" = ["F401"]  # Re-exports are intentional
"tests/*" = ["F841"]  # Unused vars in tests are fine